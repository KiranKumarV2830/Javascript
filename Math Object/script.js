// Math = It is a built in object that provides a collection of properties and methods.

let x= 3;
let y=2;
let z;

// x= Math.round(x); It rounds the number into whole number.

// x=Math.floor(x);   It rounds down the number.

// x=Math.ceil(x);   It rounds up the number.

// x=Math.trunc(x);  It removes the decimal numbers and give number.

// x = Math.pow(x,y);  It raises the number of x^y.

// x = Math.sqrt(x); It is gives square root value of the number.

// x = Math.log(x); It give log value of the number.

// x = Math.sin(x),Math.cos(x),Math.tan(x);

// x = Math.abs(x); Give absolute value of the number.

// x = Math.sign(x); Gives the sign of the number.

// x = Math.max(x,y,z);  Gives the maximum value out of three number.

// x = Math.min(x,y,z);  Gives the minimum value out of three number.

console.log(x)